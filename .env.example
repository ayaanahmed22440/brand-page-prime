# WebWarheads — environment variable names only. Never commit real values.

# Public (exposed to the browser; must be prefixed with VITE_)
VITE_APP_URL=
VITE_SUPPORT_EMAIL=

# Server-only (read inside server function handlers)
DATABASE_URL=
AUTH_SECRET=
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
EMAIL_API_KEY=
